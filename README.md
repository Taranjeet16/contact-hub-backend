# Contact Hub – Backend

Express + MongoDB backend for the Contact Management Web App.

## API Base URL
https://contact-hub-backend.onrender.com/api

## Endpoints
- GET /contacts
- POST /contacts
- PUT /contacts/:id
- DELETE /contacts/:id
